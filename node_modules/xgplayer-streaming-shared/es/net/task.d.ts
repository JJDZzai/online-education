export class Task {
    constructor(type: any, config: any);
    promise: Promise<any>;
    alive: boolean;
    _loaderType: any;
    _loader: FetchLoader | XhrLoader;
    _config: any;
    _retryCount: number;
    _retryTimer: NodeJS.Timeout;
    _canceled: boolean;
    _retryCheckFunc: any;
    _logger: any;
    exec(): Promise<any>;
    cancel(): Promise<any>;
    get running(): any;
    get loader(): FetchLoader | XhrLoader;
}
import { FetchLoader } from "./fetch";
import { XhrLoader } from "./xhr";
